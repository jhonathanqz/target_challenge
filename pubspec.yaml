name: target_challenge
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  dio: ^5.3.3
  dio_logger_request: ^1.0.0
  flutter_mobx: ^2.1.0
  flutter_modular: ^5.0.3
  mobx: ^2.2.0
  url_launcher: ^6.2.1
  shared_preferences: ^2.2.2

dev_dependencies:
  build_runner: ^2.4.6
  mobx_codegen: ^2.4.0
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

